# ─── Clay ───────────────────────────────────────────────────────────
# Webhook URL from your Clay table (Webhook source -> copy URL).
# When not set, the app uses mock enrichment data for demo purposes.
CLAY_WEBHOOK_URL=

# Optional auth token generated when you create the Clay webhook.
# Sent as "Authorization: Bearer <key>" on webhook requests.
CLAY_API_KEY=

# Base URL for Clay callback. Auto-detected on Vercel; set for other hosts.
# Clay POSTs enriched data to: <CALLBACK_BASE_URL>/api/clay-callback
CALLBACK_BASE_URL=https://tokker-mistral-hackathon.vercel.app

# ─── Mistral ────────────────────────────────────────────────────────
# API key from https://console.mistral.ai/
# Required for the PR strategy agent (step 4).
MISTRAL_API_KEY=

# ─── Privy (Auth) ───────────────────────────────────────────────────
# App ID from https://dashboard.privy.io/
# Required for TikTok / Google OAuth login.
NEXT_PUBLIC_PRIVY_APP_ID=

# ─── ElevenLabs (step 5 — voice pitches) ────────────────────────────
# API key from https://elevenlabs.io/
# ELEVENLABS_API_KEY=

# ─── x402 / Coinbase (step 5 — stablecoin payments) ─────────────────
# COINBASE_API_KEY=
NEXT_PUBLIC_PRIVY_APP_ID=
PRIVY_APP_SECRET=
